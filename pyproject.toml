[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "neuralrewardmachines"
version = "0.1.0"
description = "Neural Reward Machines Project"
authors = [{ name = "Your Name" }]
dependencies = []

[tool.setuptools.packages.find]
include = [
    "SymGroundMultiTask",
    "SymGroundMultiTask.*"
]